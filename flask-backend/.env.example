FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=replace_with_your_secret_key
DATABASE_URL=sqlite:///site.db
# Add other keys as required (e.g. AWS_ACCESS_KEY, DB_HOST) but keep values empty or placeholder
